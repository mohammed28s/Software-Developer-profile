spring:
  datasource:
    url: jdbc:h2:mem:portfolio_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

portfolio:
  admin:
    username: admin
    password: admin123
  storage:
    upload-dir: target/test-uploads
